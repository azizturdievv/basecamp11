<%= form_with(model: project) do |form| %>
  <% if project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" , style: "color:white;"%>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" , style: "color:white;" %>
    <%= form.text_field :description, class: "form-control" %>
  </div>

  <div>
 <%= form.number_field :user_id, id: :project_user_id, value: current_user.id, type: :hidden%>
  </div>

  <div>
    <%= form.submit class: "btn" %>
  </div>
<% end %>

<style>
.btn{
background: linear-gradient(180deg, #02ECFB 0%, rgba(255, 0, 229, 0.71) 100%);
  margin-top: 10px;
  color:white;
  border:none;
  padding:10px;
  border-radius: 25px;
}
.btn:hover{
  color:white;
}
</style>